---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: das-secure-message-service-web
  namespace: {{Namespace}}
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{IngressHost}}`) && PathPrefix(`/messages`)
      kind: Rule
      middlewares:
        - name: secured
      services:
        - name: das-secure-message-service-web
          port: 80
          healthCheck:
            path: /messages/health
            host: {{IngressHost}}
            intervalSeconds: 7
            timeoutSeconds: 60
  tls:
    secretName: '{{TlsSecretName}}-correct-chain'
    options:
      name: enforce-tls-12
      namespace: {{Namespace}}






